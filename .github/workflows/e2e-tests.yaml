name: Trigger e2e Tests

on:
  issue_comment:                                     
    types: [created]

env:
  GITHUB_TOKEN: ${{ secrets.ROBOT_GITHUB_TOKEN }}

jobs:
  check-for-preview-env:
    if: contains(github.event.comment.body, '/e2e') && ${{ ! contains(github.event.pull_request.labels.*.name, 'preview') }}
    runs-on: ubuntu-latest
    steps:
      - name: Post Comment
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### E2E tests did not run

              ‼️ A Preview environment deployment is needed to run e2e tests against the PR ‼️
              📖 **For more details see [Guru](https://app.getguru.com/card/TMxRRooc/Deploy-a-Preview-environment)!**
              `
            })
      - name: Terminate
        run: |
          echo "${{ ! contains(github.event.pull_request.labels.*.name, 'preview') }}"
          exit 1

  run-e2e-tests:
    if: contains(github.event.comment.body, '/e2e') && ${{ contains(github.event.pull_request.labels.*.name, 'preview') }}
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: |
          echo "${{ contains(github.event.pull_request.labels.*.name, 'preview') }}"
          echo "${{ github.event.issue.number }}"

